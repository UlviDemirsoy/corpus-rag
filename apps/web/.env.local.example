# Copy to apps/web/.env.local for local Next dev

# Browser uses same-origin /api (Next rewrite). Leave empty unless calling API cross-origin.
# NEXT_PUBLIC_API_URL=

# Used by Next server rewrites (local dev default is localhost:3001)
API_INTERNAL_URL=
